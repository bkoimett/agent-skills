# Rollback

Every production deployment should have a known recovery path.

Prefer:
- previous deploy restoration
- versioned migrations
- reversible configuration

Document irreversible operations before executing them.
