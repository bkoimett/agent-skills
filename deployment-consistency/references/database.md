# Database deployment

Validate migrations before application deployment when schema changes are included.

Use the project's database tooling and migration workflow. Avoid destructive production operations.
