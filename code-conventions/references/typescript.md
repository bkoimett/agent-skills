# TypeScript

Inspect the project's actual tsconfig and package scripts.

Prefer:
- strict typing where configured
- narrow types
- explicit public interfaces
- avoiding `any` unless justified

Do not impose strict mode on an existing project without an explicit change request.
