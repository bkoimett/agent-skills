# Project structure

Prefer a structure that makes ownership obvious.

For monorepos, isolate independently testable packages and keep shared contracts explicit.

For single applications, avoid unnecessary package boundaries.

Do not create directories merely to make the tree look sophisticated.
